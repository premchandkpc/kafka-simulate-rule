engine:
  worker_count: 8
  credits_limit: 100

nats:
  port: 4222
  jetstream: true

badger:
  dir: /var/lib/flowrule/dlq

http:
  timeout: 30s
  max_idle_conns: 100
  max_idle_per_host: 20

logging:
  level: info
